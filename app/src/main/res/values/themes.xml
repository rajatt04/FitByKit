<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FitByKit" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FitByKit" parent="Base.Theme.FitByKit" />



    <!-- M3 Expressive: Filled circle active indicator for bottom nav -->
    <style name="Widget.FitByKit.NavActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:width">56dp</item>
        <item name="android:height">56dp</item>
        <item name="android:color">?attr/colorPrimaryContainer</item>
        <item name="shapeAppearance">@style/ShapeAppearance.Material3.Corner.Full</item>
    </style>

    <style name="ToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:fontFamily">@font/poppins</item>
    </style>

    <!-- M3 Expressive: Letter browse chip for A-Z in Diet bottom sheet -->
    <style name="LetterChip" parent="Widget.Material3.Chip.Suggestion">
        <item name="android:minWidth">40dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">13sp</item>
        <item name="chipCornerRadius">20dp</item>
        <item name="checkedIconVisible">false</item>
    </style>

</resources>
